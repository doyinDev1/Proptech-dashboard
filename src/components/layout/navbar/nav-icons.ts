import { ApplicationsIcon, DashboardIcon, ListingsIcon, RequestIcon, UsersIcon, TaskIcon } from "../../svgs";

export const navIcons = {
  dashboard: DashboardIcon,
  listings: ListingsIcon,
  users: UsersIcon,
  request: RequestIcon,
  applications: ApplicationsIcon,
  tasks: TaskIcon
};
